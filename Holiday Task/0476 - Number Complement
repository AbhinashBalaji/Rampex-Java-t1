class Solution {
    public int findComplement(int num) {
        int res=0,pos=0;
        while(num>0){
        int bit=(num&1)^1;
        res=res|(bit<<pos);
        pos++;
        num=num>>1;
        }
        return res;
    }
}
